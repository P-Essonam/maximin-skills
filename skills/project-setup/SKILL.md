---
name: project-setup
description: Create a new shadcn TanStack Start monorepo in the user's Music folder, add the agreed ESLint architecture boundaries, validate it, and publish the initial project to a private GitHub repository. Use for starting a new project with this stack, not for adding authentication, Convex, UI libraries, CI, or deployment to an existing project.
---

# Project Setup

Ask only for the project name. Resolve the operating system's Music folder and use `<Music>/<project-name>` as the destination.

## Create the project

1. Check that Node.js, pnpm, and Git are available. If one is missing, help the user install or configure it, verify it, and continue.
2. Ensure the destination does not contain an existing project.
3. From the Music folder, run exactly:

   ```powershell
   pnpm dlx shadcn@latest init --preset b0 --template start --monorepo --pointer
   ```

4. Supply the approved project name when prompted.
5. Keep the structure and configuration generated by shadcn. Verify them; do not reorganize or replace them.

## Add architecture boundaries

Locate the generated TanStack Start application. Install `eslint-plugin-boundaries` in that workspace and integrate the bundled [ESLint boundaries configuration](assets/eslint-boundaries.config.js) with its existing ESLint flat config. Read it from the installed skill; do not fetch a remote copy. Preserve the generated ESLint configuration and append the exported configurations.

Keep feature code under folders inside `src/features/<feature-name>/`, typically `components`, `hooks`, and `lib`. Put feature-specific types, utilities, schemas, and constants in that feature's `lib` folder. Do not require barrel or index files.

The rules must enforce:

- source files belong to a recognized application area;
- files directly under a feature root are rejected, so the feature root contains folders only;
- one feature cannot import another feature;
- global `src/components`, `src/hooks`, `src/lib`, and `src/server` code cannot import feature code;
- code shared across features moves to the matching global folder;
- files within the same feature can import each other.

Classify any extra root entry files generated by the current shadcn template as `app-root`, and ignore generated TanStack files. Do not move generated files to satisfy lint.

## Validate and publish

From the monorepo root, use the scripts generated by shadcn to install dependencies, lint, typecheck, and build. Start the development server and confirm the starter page responds locally. Confirm ESLint rejects a temporary cross-feature import, then remove the temporary test files.

After every check passes, initialize Git, create the initial commit, and publish the project as a private GitHub repository. Check GitHub CLI availability and authentication at this stage; help configure either if needed. Never make the repository public.

Report the project path, checks run, private repository URL, and development command. Report failures with their real error and stop before committing or publishing an invalid project.
